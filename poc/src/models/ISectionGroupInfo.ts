import ISectionInfo from "./ISectionInfo";
export default interface ISectionGroupInfo {
    // #region Properties (3)

    sections: ISectionInfo[];
    selectorHeight: number;
    selectorTop: number;

    // #endregion Properties (3)
}
